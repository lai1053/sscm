server:
  port: ${SERVER_PORT:0}
spring:
  profiles:
    include: core,dev
  application:
    name: admin
  cloud:
    nacos:
      config:
        enabled: true
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        prefix: admin
      discovery:
        enabled: true
        server-addr: 127.0.0.1:8848
